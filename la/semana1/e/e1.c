#include <stdio.h>

int main() {
// i -> checa se o numero existe ou nao existe, se existir printa "number EXISTS" se não existir o número é inserido
// d -> dá delete ao numero se existir na hash table e printa "OK", caso contrário printa "NO"
// c -> checa se o numero esta na tabela e printa a sua localizaçao, caso contrario printa "NO"
// p -> printa a tabela omitindo os espaços vazios

    int i, d, c;
    
    
    return 0;
}